import React from 'react';
import GitHubIcon from '@/assets/socials/github.svg';

interface IconProps {
    className?: string;
}

const GitHubIconComponent: React.FC<IconProps> = ({ className }) => {
    // return <GitHubIcon className={className} />;
    return <></>
};

export default GitHubIconComponent;